# 复制本文件为 application-local.yml（已被 gitignore），填入 DeepSeek API Key
# 或在启动后端前设置环境变量：$env:DEEPSEEK_API_KEY="sk-你的密钥"

langchain4j:
  open-ai:
    chat-model:
      api-key: sk-你的DeepSeek密钥
    streaming-chat-model:
      api-key: sk-你的DeepSeek密钥

# Pexels 图片素材 API（可选，用于 AI 工作流素材收集）
# 或在启动前：$env:PEXELS_API_KEY="你的Pexels密钥"
app:
  pexels-api-key: 你的Pexels密钥
